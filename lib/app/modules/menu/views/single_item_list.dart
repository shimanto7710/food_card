import 'package:flutter/material.dart';

class SingleItemList extends StatelessWidget {
  const SingleItemList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
